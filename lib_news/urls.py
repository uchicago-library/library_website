from django.urls import path
from django.http import HttpResponse

# from views import RSSViews

urlpatterns = [
    # path('binky', HttpResponse("Thingamajig")),
    # path(r'^category/(?P<slug>[-\w]+)/rss/$', views.RSSViews()),
]

